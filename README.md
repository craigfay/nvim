
## System Dependencies (Assuming MacOS)

NVIM v0.10.4: `brew install nvim`

RipGrep `brew install ripgrep`

LuaRocks `brew install luarocks`

## Commands

update plugins: `nvim --headless "+Lazy! sync" +qa`
